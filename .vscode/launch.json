{
    "version": "0.2.0",
    
    "configurations": [
        {
            "name": "MCP Server (FastMCP)",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/main.py", // Или main.py, если файл так называется
            "console": "integratedTerminal",
            "justMyCode": true,
            "env": {
                "PYTHONUNBUFFERED": "1" // Чтобы логи сразу появлялись
            }
        }
    ]
}